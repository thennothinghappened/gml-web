/// @param {string} base_url
function Router(base_url) : Route(base_url) constructor {
	
	/// @desc Handle the route if it belongs to us
	/// @param {Id.Request} req
	/// @param {Id.Response} res
	/// @param {Function} next
	function handle(req, res, next) {
		
	}
	
	
	
}